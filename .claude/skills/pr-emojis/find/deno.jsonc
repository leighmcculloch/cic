{
  // Find GitHub PRs the user has acted on recently, via the events API.
  "tasks": {
    "find": "deno run --allow-net=api.github.com --allow-env=GITHUB_TOKEN ./main.ts"
  },
  "fmt": {
    "lineWidth": 100
  }
}
